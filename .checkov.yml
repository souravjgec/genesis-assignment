skip-check:
  # Lambda intentionally stays outside a VPC for this project.
  - CKV_AWS_117

  # Reserved concurrency is intentionally unset because this AWS account cannot reserve concurrency without violating its minimum unreserved concurrency requirement.
  - CKV_AWS_115

  # Function URL is intentionally public so demos and smoke tests can call it directly.
  - CKV_AWS_258

  # Public Lambda invoke permissions are required because the Function URL auth type is NONE.
  - CKV_AWS_301
